# সম্পূর্ণ অ্যাপে লাইট ও ডার্ক থিম বাস্তবায়ন পরিকল্পনা (Theme Architecture Plan)

## ১. সারসংক্ষেপ ও লক্ষ্য
মেসার্স জান্নাত সুজ অ্যাপ্লিকেশনে একটি প্রফেশনাল, আরামদায়ক ও নিখুঁত ডুয়াল থিম (Light / Dark Theme) ব্যবস্থা গড়ে তোলা।
**প্রধান নিশ্চয়তা**:
- কোনো অবস্থাতেই ফন্ট কালার ব্যাকগ্রাউন্ডের সাথে মিশে ইনভিজিবল বা অস্পষ্ট হবে না।
- প্রতিটি বাটন, কার্ড, টেবিল, ইনপুট বক্স, ড্রপডাউন এবং মোডালের কনট্রাস্ট রেশিও সুস্পষ্ট থাকবে।
- জান্নাত সুজ-এর সিগনেচার ব্র্যান্ড গোল্ডেন/অ্যাম্বার অ্যাকসেন্ট লাইট ও ডার্ক উভয় মোডেই মানানসই হবে।

---

## ২. কালার সিস্টেম ও গ্রেডিং আর্কিটেকচার (Tailwind v4 CSS ভেরিয়েবলস)

ক্লাসের পরিবর্তে সরাসরি CSS ভ্যারিয়েবল ভিত্তিক কালার সিস্টেম ব্যবহার করা সবচেয়ে নিরাপদ এবং ক্লিন। এতে প্রতিটি উপাদানের কালার অটোমেটিক থিম অনুযায়ী সুইচ হবে:

| এলিমেন্ট প্রকার | ডার্ক থিম (Dark Mode) | লাইট থিম (Light Mode) | উদ্দেশ্য ও সতর্কতা |
| :--- | :--- | :--- | :--- |
| **মূল অ্যাপ ব্যাকগ্রাউন্ড** | `#020617` (slate-950) | `#f8fafc` (slate-50) | ব্যাকগ্রাউন্ড অতিরিক্ত সাদা না হয়ে চোখের জন্য আরামদায়ক সফট অফ-হোয়াইট |
| **সারফেস / কার্ড ব্যাকগ্রাউন্ড** | `#0f172a` (slate-900) / `#090e1a` | `#ffffff` (pure white) | কার্ডগুলো ব্যাকগ্রাউন্ড থেকে স্পষ্টভাবে পৃথক থাকবে |
| **সেকেন্ডারি কন্টেইনার** | `#1e293b` (slate-800) | `#f1f5f9` (slate-100) | ইনার বক্স, ব্যাজ ও সার্চ বার |
| **প্রাইমারি টেক্সট (মূল ফন্ট)** | `#f8fafc` (slate-50 / white) | `#0f172a` (slate-900 / dark slate) | **১০০% স্পষ্ট ও উচ্চ কনট্রাস্ট** |
| **সেকেন্ডারি টেক্সট (সাব-লেবেল)**| `#94a3b8` (slate-400) | `#475569` (slate-600) | অস্পষ্টতা দূর করতে লাইট মোডে যথেষ্ট ডিপ টেক্সট |
| **মিউটেড টেক্সট (তারিখ/হিন্ট)** | `#64748b` (slate-500) | `#64748b` (slate-500) | স্পষ্ট পাঠযোগ্যতা |
| **বর্ডার ও ডিভাইডার** | `#334155` (slate-700) / `slate-800` | `#e2e8f0` (slate-200) | হালকা ও প্রিমিয়াম বিভাজন লাইন |
| **ইনপুট ফিল্ড ও ড্রপডাউন** | `#020617` ব্যাকগ্রাউন্ড, `#f8fafc` টেক্সট | `#ffffff` ব্যাকগ্রাউন্ড, `#0f172a` টেক্সট | টাইপ করার সময় টেক্সট সম্পূর্ণ দৃশ্যমান থাকবে |
| **অ্যাকসেন্ট (অ্যাম্বার/গোল্ডেন)** | `#f59e0b` (amber-500) / `#fcd34d` | `#d97706` (amber-600) / `#b45309` (amber-700) | লাইট মোডে ফ্যাকাশে না হয়ে ডিপ গোল্ডেন/ব্রোঞ্জ লুক দিবে |
| **সাকসেস (সবুজ)** | `#10b981` (emerald-500) | `#059669` (emerald-600) | পেইড ও ফ্রি স্টক স্টেটাসে স্পষ্ট |
| **ডেঞ্জার/অ্যালার্ট (লাল/গোলাপি)**| `#f43f5e` (rose-500) / `#ef4444` | `#e11d48` (rose-600) / `#dc2626` | বকেয়া ও কম স্টক ওয়ার্নিংয়ে সুস্পষ্ট |

---

## ৩. প্রযুক্তিগত কৌশল ও কম্পোনেন্ট তালিকা

### ধাপ ১: কোর থিম ইঞ্জিন (`ThemeContext`)
- `src/contexts/ThemeContext.tsx`:
  - `theme`: `'dark' | 'light'`
  - `toggleTheme()`: ওয়ান ক্লিক টগল
  - `localStorage` পারসিস্টেন্স (`jannat_shoes_theme`)
  - `html` এলিমেন্টে `dark` অথবা `light` ক্লাস ও `data-theme` সেট করা।
- `src/index.css`:
  - গ্লোবাল থিম রুলস ও স্ক্রলবার কালার অ্যাডাপ্টেশন।

### ধাপ ২: ফ্রেমওয়ার্ক ও নেভিগেশন কম্পোনেন্টস
1. **`Header.tsx`**:
   - হেডার ব্যাকগ্রাউন্ড, ডেট ব্যাজ, শপ নেম, এবং লাইট/ডার্ক টগল বাটন (Sun/Moon)।
2. **`Sidebar.tsx`**:
   - ডেস্কটপ সাইডবার ব্যাকগ্রাউন্ড, অ্যাক্টিভ ও ইনঅ্যাক্টিভ মেনু আইটেমের টেক্সট ও আইকন কালার।
3. **`Navigation.tsx`**:
   - মোবাইল বটম নেভিগেশন বার, গ্লাসফেক্ট ব্যাকগ্রাউন্ড ও সিলেকশন স্টেট।

### ধাপ ৩: প্রধান কোর পেজ ও ড্যাশবোর্ড
1. **`Dashboard.tsx`**:
   - সব মেট্রিক কার্ড (আজকের বিক্রি, মোট স্টক, মোট বকেয়া, ক্যাশ ড্রয়ার ইত্যাদি)।
   - চার্ট ব্যাকগ্রাউন্ড ও অ্যাক্সিস টেক্সট কালার।
   - কুইক অ্যাকশন বাটনসমূহ।
2. **`StockManagement.tsx`**:
   - ইউনিফাইড ৩D ওভারভিউ কার্ড, ফিল্টার বার, প্রোডাক্ট টেবিল/গ্রিড, এডিট ও ক্রিয়েট ফর্ম।
3. **`DueManagement.tsx` (বাকি খাতা)**:
   - কাস্টমার সামারি কার্ড, বকেয়া কালেকশন মডাল, পেমেন্ট হিস্ট্রি টেবিল।
4. **`PosOrderBuilder.tsx` ও `PendingOrders.tsx`**:
   - জুতার সাইজ সিলেকশন গ্রিড, আইটেম কার্ড, মেমো সামারি, ক্যাশ/বাকি পেমেন্ট ইনপুট।
5. **`SalesHistory.tsx` ও `Reports.tsx`**:
   - ক্যাশ বুক, প্রফিট ও সেলস অ্যানালাইসিস টেবিল ও গ্রাফ।

### ধাপ ৪: মোডাল ও ওভারলে কম্পোনেন্টস
1. **`InvoiceModal.tsx`**:
   - চালানের পেপার ভিউ (অনস্ক্রিন ও প্রিন্ট উভয় অবস্থাতেই ক্রিস্টাল ক্লিয়ার ব্ল্যাক-অন-হোয়াইট)।
2. **`DeliveryConfirmModal.tsx`, `EditPendingOrderModal.tsx`, `LowStockModal.tsx`**:
   - পপআপ উইন্ডো, ড্রপডাউন এবং কনফার্মেশন বাটন।
3. **`ShopManagement.tsx`, `UserManagement.tsx`, `TrashManagement.tsx`**:
   - অ্যাডমিন ও শপ সেটিং স্ক্রিন।

---

## ৪. ফন্ট অদৃশ্য হওয়া রোধে নির্দিষ্ট চেকপয়েন্ট (Anti-Invisible Rules)
1. **হালকা ব্যাকগ্রাউন্ডে কখনো সাদা লেখা নয়**: কোনো উপাদানে `bg-white` বা `bg-slate-100` হলে টেক্সট অবশ্যই `text-slate-900` বা `text-slate-800` হতে হবে।
2. **গাঢ় ব্যাকগ্রাউন্ডে কখনো কালো লেখা নয়**: `bg-slate-900` হলে টেক্সট `text-white` বা `text-slate-100` নিশ্চিত করা।
3. **ইনপুট ও টেক্সটএরিয়া স্টাইল**: প্রতিটি ফর্ম ফিল্ডে `bg-white dark:bg-slate-950 text-slate-900 dark:text-slate-100 border-slate-300 dark:border-slate-800` ব্যবহার বাধ্যতামূলক।
4. **টেবিল হেডার ও বর্ডার**: টেবিল হেডারে লাইট মোডে সফট গ্রে ব্যাকগ্রাউন্ড (`bg-slate-100 dark:bg-slate-900/80`) এবং ডিপ গ্রে হেডার টেক্সট (`text-slate-700 dark:text-slate-300`)।

---

## ৫. বাস্তবায়ন ধাপসমূহ ও বর্তমান অগ্রগতি
- [x] **ফেজ ১**: `ThemeContext.tsx` তৈরি এবং `main.tsx` ও `index.css`-এ থিম সাপোর্ট যুক্ত করা। (সম্পন্ন)
- [x] **ফেজ ২**: `Header.tsx`, `Sidebar.tsx` ও `Navigation.tsx`-এ থিম টগল সুইচ ও কালার গ্রেডিং যুক্ত করা। (সম্পন্ন)
- [x] **ফেজ ৩**: `Dashboard.tsx` এবং `StockManagement.tsx`-এ পরিপূর্ণ লাইট/ডার্ক কালার অ্যাডাপ্টেশন। (সম্পন্ন)
- [x] **ফেজ ৪**: `DueManagement.tsx`, `PosOrderBuilder.tsx`, `PendingOrders.tsx`, `SalesHistory.tsx`, `Reports.tsx`, `ShopManagement.tsx`, `UserManagement.tsx` ও মোডালসমূহে কালার গ্রেডিং ফাইনাল করা। (সম্পন্ন)
- [x] **ফেজ ৫**: `TrashManagement.tsx` কালার গ্রেডিং, কনট্রাস্ট ও অ্যান্টি-ইনভিজিবল টেস্ট সম্পন্ন। সম্পূর্ণ অ্যাপের কোড কম্পাইলেশন সফল। (সম্পন্ন)
